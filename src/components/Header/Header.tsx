import Main from "./MainHeader";

/**
 * @component Header
 * @description The Header component provides a modular structure for the header section of the layout.
 */
const Header = {
  Main
};

export default Header;
