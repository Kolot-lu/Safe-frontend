import { FixedWidth } from './FixedWidth';
import { FullWidth } from './FullWidth';

/**
 * @component PageLayout
 * @description PageLayout provides two layout options: FixedWidth and FullWidth, 
 * allowing flexible layout structures for different page needs.
 */
const PageLayout = {
  FixedWidth,
  FullWidth
};

export default PageLayout;
